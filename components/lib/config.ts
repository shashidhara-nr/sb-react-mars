export const accountNumberlayout = [2, 2, 3, 3, 1, 1];

export enum SnackbarTheme {
  Info = 'info',
  Warning = 'warning',
  Success = 'success',
}

export enum CardSize {
  small = 328,
  medium = 360,
}

export enum RowLayout {
  one = 1,
  two,
  three,
  four,
  five,
  six,
  seven,
  eight,
  nine,
  ten,
  eleven,
  twelve,
}

export enum GridGap {
  none = 0,
  small = 3,
  medium = 3.5,
}

export enum Columns {
  mobile = 4,
  tablet = 8,
  desktop = 12,
}
